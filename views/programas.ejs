<!Doctype html>
<html>

<head>
    <% include ./partials/head %>
    <link rel="stylesheet" href="stylesheets/index.css">
    <link rel="stylesheet" href="stylesheets/programas.css">
    <title>Programas</title>
</head>
<body>
    <% include ./partials/menuNav %>
    <main class="container-fluid mx-auto pr-0 pl-0">
      <section id="parallax01">
        <div class="container">
          <div class="wtitle text-center text-light">
            <h2>Nuestros programas</h2><hr><p class="text-muted text-capitalize">Nosotros tambien contamos I.A.P</p>
          </div>
        </div>
      </section>
      <section class="h-100 mt-5 position-relative">
        <div class="container-fluid shadow rounded">
          <div class="row">
            <nav class="col-md-3 col-12 border-bottom border-warning mb-5 mb-md-0 "  id="spy">
                <ul class="nav nav-pills flex-column">
                  <li class="nav-item"><a class="nav-link active" href="#scroll1">Programas de salud</a></li>
                  <li class="nav-item"><a class="nav-link" href="#scroll2">Alimentación y becas de desayunos Escolares</a></li>
                  <li class="nav-item"><a class="nav-link" href="#scroll3">Donativos y material didáctico</a></li>
                  <li class="nav-item"><a class="nav-link" href="#scroll4">Talleres de Capacitación Laboral</a></li>
                  <li class="nav-item"><a class="nav-link" href="#scroll5">Inserción Laboral</a></li>
                </ul>
            </nav>
            <div class="col-md-9  col-12 scrollspy-example" data-spy="scroll" data-target="#spy" data-offset="0">
              <section id="parallax02">
                <div class="container">
                  <div class="pt-5 pb-5 text-center text-light">
                    <h2>Programa de Salud</h2><hr class="col-3"><p class="text-muted text-capitalize">Nosotros tambien contamos I.A.P</p>
                  </div>
                </div>
              </section>
              <div id="scroll1">
                <h2>First Section</h2>
                <p>
                During Compile time, the compiler converts the source code into Microsoft Intermediate Language (MSIL). Microsoft Intermediate Language (MSIL) are CPU-Independent set of instructions that can be effectively converted to the native code. Now with the help of JIT compiler, IL code can be executed on any computer architecture supported by the JIT compiler.
                </p>
              </div>
              <div id="scroll2">
                <h2></h2>
                <p>
                During Compile time, the compiler converts the source code into Microsoft Intermediate Language (MSIL). Microsoft Intermediate Language (MSIL) are CPU-Independent set of instructions that can be effectively converted to the native code. Now with the help of JIT compiler, IL code can be executed on any computer architecture supported by the JIT compiler.
                </p>
              </div>
              <div id="scroll3">
                <h2>Third Section</h2>
                <p>
                During Compile time, the compiler converts the source code into Microsoft Intermediate Language (MSIL). Microsoft Intermediate Language (MSIL) are CPU-Independent set of instructions that can be effectively converted to the native code. Now with the help of JIT compiler, IL code can be executed on any computer architecture supported by the JIT compiler.
                </p>
              </div>
              <div id="scroll4">
                <h2>Fourth Section</h2>
                <p>
                During Compile time, the compiler converts the source code into Microsoft Intermediate Language (MSIL). Microsoft Intermediate Language (MSIL) are CPU-Independent set of instructions that can be effectively converted to the native code. Now with the help of JIT compiler, IL code can be executed on any computer architecture supported by the JIT compiler.
                </p>
                <p>
                During Compile time, the compiler converts the source code into Microsoft Intermediate Language (MSIL). Microsoft Intermediate Language (MSIL) are CPU-Independent set of instructions that can be effectively converted to the native code. Now with the help of JIT compiler, IL code can be executed on any computer architecture supported by the JIT compiler.
                </p>
                <p>
                During Compile time, the compiler converts the source code into Microsoft Intermediate Language (MSIL). Microsoft Intermediate Language (MSIL) are CPU-Independent set of instructions that can be effectively converted to the native code. Now with the help of JIT compiler, IL code can be executed on any computer architecture supported by the JIT compiler.
                </p>
                <p>
                During Compile time, the compiler converts the source code into Microsoft Intermediate Language (MSIL). Microsoft Intermediate Language (MSIL) are CPU-Independent set of instructions that can be effectively converted to the native code. Now with the help of JIT compiler, IL code can be executed on any computer architecture supported by the JIT compiler.
                </p>
              </div>
            </div>
          </div>
        </div>

      </section>
    </main>
    <% include ./partials/footer %>
    <% include ./partials/scripts%>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/modernizr/2.8.3/modernizr.min.js" type="text/javascript"></script>
    <script type="text/javascript" src="/javascripts/programas.js"> </script>
</body>
</html>